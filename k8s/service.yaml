apiVersion: v1
kind: Service
metadata:
  name: my-service
spec: 
  selector:
    app: azure-service-app
  type: NodePort
  ports:
   - name: http
     port: 80
     targetPort: 80
     nodePort: 30036
     protocol: TCP